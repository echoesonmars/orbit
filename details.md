# 🚀 OrbitAI: Technical Architecture & Project Specification

## 📌 О проекте (Elevator Pitch)
**OrbitAI** — это модульная AI-платформа для принятия решений и монетизации космических данных. Платформа переводит сложные инженерные расчеты космических миссий в понятные бизнес-метрики, оценивает коммерческую ценность спутниковых снимков до их создания и генерирует технические задания с помощью ИИ.

---

## 🛠 Технологический стек (Senior-уровень)

Проект построен по принципу **Модульного Монорепозитория (Monorepo)** с разделением зон ответственности (Microservices approach).

1. **Frontend (Клиент):** Next.js latest (App Router), React, Tailwind CSS v3, Shadcn UI, Zustand, Leaflet/Mapbox (для карт).
2. **API Gateway (Шлюз):** Node.js, Express, TypeScript, Zod (валидация).
3. **ML Core (ИИ-движок):** Python 3.11, FastAPI, Pydantic, Scikit-learn/LightGBM, Rasterio, Geopandas, OpenAI API.
4. **Database & Auth (Данные):** Supabase (PostgreSQL + PostGIS для гео-запросов), Supabase Auth (JWT), Supabase Storage (S3).
5. **Infrastructure:** Docker, Docker Compose, Turborepo (опционально для управления монорепозиторием).

---

## 🧩 Ключевые модули платформы

1. **Data Hub:** Ингресс и хранение геоданных (TLE, Sentinel, Landsat) в PostGIS. *(Код/SQL)*
2. **Mission Designer:** Генерация ТЗ миссии на основе бизнес-целей (NLP). *(LLM: OpenAI API)*
3. **Capture Value Predictor:** Прогноз коммерческой ценности снимка до съемки. *(Custom ML: LightGBM)*
4. **Report Generator:** Генерация объяснимых (explainable) PDF-отчетов. *(Python/ReportLab + LLM)*
5. **Launch Delay Predictor:** Прогноз задержек запуска ракет. *(Custom ML/Time-series)*
6. **Orbit Optimizer:** Оптимизация маневров спутника. *(Математика/Алгоритмы)*
7. **Orbit Suitability Scorer:** Оценка орбиты под коммерческую задачу. *(Математика)*
8. **Failure Forensics:** Поиск причин аномалий по телеметрии. *(ML/Аналитика)*
9. **Scenario Simulator:** Симулятор миссий "Что если". *(Алгоритмы)*
10. **ESG Assessor:** Расчет углеродного следа и эко-рейтинга. *(Код/Формулы)*

---

## 📂 Структура кодовой базы (Monorepo)

Ниже представлено детальное описание каждой папки и файлов внутри монорепозитория.

### `apps/web/` (Фронтенд — Next.js)
Отвечает исключительно за UI/UX. Не содержит сложной бизнес-логики ИИ.
* `/src/app`: Страницы (Routing). `page.tsx` (Главная), `dashboard/page.tsx` (Рабочая панель).
* `/src/components`: UI-компоненты.
  * `/ui`: Базовые компоненты Shadcn (Кнопки, Инпуты, Модалки).
  * `/map`: Компоненты Leaflet/Mapbox для отрисовки полигонов и орбит.
  * `/forms`: Формы ввода бизнес-целей для Mission Designer.
* `/src/hooks`: Кастомные React хуки (например, `useAuth`, `usePredictValue`).
* `/src/store`: Глобальный стейт Zustand (хранение выбранных координат, текущего отчета).
* `/src/lib`: Утилиты (подключение к Supabase Client, форматирование дат).

### `services/gateway/` (Управляющий бэкенд — Node.js)
Оркестратор. Принимает трафик, проверяет юзеров, маршрутизирует задачи в Python.
* `/src/controllers`: Обработка запросов (например, принять координаты, запросить Python, вернуть ответ).
* `/src/middleware`: Защита роутов. Проверка JWT токенов из Supabase. Rate limiting.
* `/src/routes`: Эндпоинты API (например, `POST /api/v1/missions`).
* `/src/services`: HTTP-клиенты (Axios) для общения с `ml-api` сервисом.
* `/src/validators`: Схемы Zod для жесткой проверки входящих данных (защита от "мусора").

### `services/ml-api/` (ИИ и Расчеты — Python/FastAPI)
Математическое ядро. Сюда нет прямого доступа из интернета, только через `gateway`.
* `/app/main.py`: Точка запуска FastAPI приложения.
* `/app/api/endpoints/`: Роуты FastAPI (например, `@app.post("/predict")`).
* `/app/core/`: Конфигурация (загрузка переменных окружения, настройки логов).
* `/app/modules/`: Изолированная логика каждого из 10 модулей:
  * `mission_designer.py` (Работа с промптами и OpenAI).
  * `value_predictor.py` (Предобработка фичей и запуск модели).
  * `geo_processor.py` (Функции PostGIS, Rasterio, работа с BBox).
* `/app/models/`: Сохраненные веса обученных моделей (файлы `.pkl`, `.joblib`).
* `/app/schemas/`: Pydantic модели (валидация данных внутри Python).
* `requirements.txt`: Зависимости (fastapi, uvicorn, scikit-learn, geopandas).

### `shared/` (Общие контракты)
Синхронизация между TypeScript (Gateway) и Python (ML-API).
* `/types`: Общие интерфейсы данных. Если Python возвращает `{"score": 90}`, здесь лежит TS-интерфейс `interface IPrediction { score: number; }`. Гарантирует, что фронт и бэк "говорят на одном языке".

### `infra/` (Инфраструктура и Деплой)
Файлы для развертывания проекта.
* `/docker/`: `Dockerfile.web`, `Dockerfile.gateway`, `Dockerfile.ml`.
* `/db/`: SQL-скрипты миграций для Supabase (создание таблиц `missions`, `predictions`, настройка PostGIS).

### ⚙️ Файлы в корне проекта (Root)
* `docker-compose.yml`: Скрипт локального запуска всей системы (Postgres, Gateway, ML-API) одной командой.
* `package.json`: Корневой файл для управления воркспейсами (Turborepo или pnpm workspaces).
* `.env.example`: Шаблон секретных ключей (API базы, OpenAI).
* `.cursorrules`: Правила для AI-редактора Cursor для поддержания чистоты архитектуры.

---

## 🔄 Пример Data Flow (Прогноз ценности снимка)

1. **Web (Next.js):** Юзер рисует квадрат на карте. Отправляется POST-запрос с BBox-координатами на `gateway`.
2. **Gateway (Node.js):** Мидлвар проверяет сессию Supabase Auth. Zod валидирует правильность BBox.
3. **Gateway -> ML-API:** Node.js делает внутренний HTTP POST-запрос в Python контейнер.
4. **ML-API (Python):** Pydantic проверяет данные. Скрипт делает SQL-запрос в Supabase (PostGIS) для поиска погодных условий. Запускается LightGBM модель.
5. **Response:** Python возвращает JSON `{"value_score": 85, "factors": [...]}`. Node.js логирует это в БД и прокидывает на фронт. UI обновляет дашборд.