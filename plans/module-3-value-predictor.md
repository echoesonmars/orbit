# 💎 Модуль 3: Capture Value Predictor (Предсказатель Коммерческой Ценности Снимка)

**Capture Value Predictor** — это "мозг монетизации" платформы OrbitAI. 
Его главная задача — ответить инвестору или оператору спутника на вопрос: *"Если я потрачу топливо спутника и сфотографирую этот квадрат на Земле прямо сейчас, за сколько долларов я смогу продать этот снимок на бирже?"*

Это классическая задача **Машинного Обучения (Регрессия)** — предсказание конкретного числа (цены) на основе множества факторов (фичей).

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Мы должны хранить результаты предсказаний и историю того, какие координаты пользователи запрашивали чаще всего (это полезная аналитика!).

**Задачи по настройке БД (Supabase PostGIS):**
*   [ ] Создать таблицу `predictions_history`, где будут храниться:
    *   `id` (Уникальный номер).
    *   `user_id` (Кто запрашивал).
    *   `target_bbox` (Геометрия квадрата - полигон).
    *   `predicted_value_usd` (Сколько долларов предсказал ИИ).
    *   `factors_json` (Почему ИИ так решил? Например: "Облаков: 5%, Сезон: Сбор урожая").
*   [ ] Создать *View* (виртуальную таблицу) в Supabase для аналитики: "Самые горячие (дорогие) точки на планете за месяц".

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI)

Этот модуль (`services/ml-api/app/modules/value_predictor.py`) — это обертка вокруг нашей обученной модели. Он не учит модель на лету, он **использует** уже готовую модель.

**Задачи по разработке API (Inference Pipeline):**
*   [ ] **Интеграция с Погодой:** Написать асинхронную функцию `get_weather_forecast(lat, lon, date)`. Если спутник пролетает завтра, а там гроза — цена оптического снимка равна $0 (на нем будут только облака).
*   [ ] **Подгрузка Весов Модели:** В FastAPI при старте сервера (`app/main.py` на событии `startup`) загрузить файл `value_model_v1.pkl` в оперативную память (из папки `app/models/`). Это нужно, чтобы каждый запрос не читал файл с жесткого диска (экономим миллисекунды).
*   [ ] **Препроцессинг на лету:** Функция, которая берет координаты от юзера, стягивает погоду, стягивает тип местности из Data Hub (Модуль 1), превращает всё это в список цифр (массив `[5.2, 1, 0.89...]`) и скармливает загруженной модели.
*   [ ] **Эндпоинт `POST /api/predict-value`:** Возвращает JSON:
    ```json
    {
      "value_usd": 1250,
      "confidence_score": 0.88,
      "factors": { "cloud_penalty": "-$500", "location_premium": "+$800" }
    }
    ```

---

## 3. 🤖 Что нужно обучать (ML & Data Science)

В отличие от Модуля 2 (где мы брали готовый ChatGPT), здесь мы **обязаны** обучить свою собственную модель (Custom ML). OpenAI не умеет предсказывать цены на спутниковые снимки, это узкая ниша.

### 🎓 БАЗА: Как мы научим ИИ предсказывать цены (Обучение с учителем / Supervised Learning)

**Шаг 1: Создание Датасета (Таблицы для обучения)**
Нам нужна таблица (csv-файл) на минимум 5,000-10,000 строк. Каждая строка — это исторический факт продажи снимка. Подразумевается, что мы собрали эту статистику через Data Hub или купили базу данных у бирж (SkyWatch, UP42).
*   *Колонка 1 (Облачность):* 12%
*   *Колонка 2 (Разрешение/GSD):* 0.5 метра (крупный план)
*   *Колонка 3 (Тип зоны):* 1 (Сельское хозяйство), 2 (Порт/Город), 3 (Океан/Пустыня). За снимок океана никто не платит.
*   *Колонка 4 (События):* Есть ли война/катастрофа в этом регионе сейчас? (1 = Да, 0 = Нет). *Снимки из зон ЧС стоят в 10 раз дороже.*
*   **КОЛОНКА `Y` (Целевая переменная):** 850 долларов. (Фактическая цена продажи).

**Шаг 2: Выбор Алгоритма**
Так как у нас табличные данные (строки и столбцы), Нейронные сети (Deep Learning) здесь избыточны и будут работать хуже. Идеальный выбор — ансамблевые методы, градиентный бустинг.
*   [ ] Будем использовать **XGBoost**, **LightGBM** или **CatBoost** (алгоритм от Яндекса, отлично понимает текстовые категории вроде "Город").

**Шаг 3: Обучение (Код Data Scientist'а)**
Python код будет искать скрытые математические зависимости. ИИ сам поймет, что "Разрешение 0.5м" увеличивает цену экспоненциально, а "Облачность > 30%" сбрасывает её в ноль.
После тренировки (это займет 10 минут на ноутбуке), мы проверим модель на отложенных данных (Тестирование). Если она предсказывает цену $850, а реальная была $830 — это супер-результат (ошибка Mean Absolute Error (MAE) = $20).

**Шаг 4: Экспорт**
Скрипт сохранит этот "опыт" в бинарный файл (`.pkl` или `.joblib`). Именно этот файл мы положим в `services/ml-api/app/models/` для Модуля 2.

---

## 4. 🌐 Интеграция с Frontend и Gateway

Это самая красивая и "продающая" фича для инвесторов.

*   [ ] **Frontend (Next.js):**
    *   На странице `app/dashboard/page.tsx` рисуем карту (Leaflet/Mapbox). 
    *   Пользователь нажимает инструмент "Прямоугольник", выделяет зону над Нью-Йорком.
    *   Слева выезжает панель (Sidebar), где крутится модный "скелетон" загрузки (Loading state).
    *   Через секунду появляется большая цифра: **Эстимейт: $1,250** и красивый график-бублик (Recharts), показывающий, что $800 стоит сам город, а штраф за облачность -$150.
*   [ ] **Node.js Gateway:**
    *   Контроллер получает прямоугольник (BBox) от фронтенда.
    *   С помощью `Zod` убеждается, что юзер не выделил карту размером с весь земной шар (потому что запрос погоды для всей Земли сломает сервер). Накладываем ограничение: максимум 100 кв. км за 1 запрос.
    *   Отправляет координаты в Python ML-API и перенаправляет JSON-ответ обратно на клиент.

---

**Резюме:** Третья функция работает по принципу классического машинного обучения (Табличная регрессия). Её ценность в том, что она берет сложную неочевидную аналитику (наложение погоды, разрешения камеры и типа земли) и выдает бизнесу ОДНУ мгновенную цифру в понятной валюте.
