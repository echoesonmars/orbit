# 🎯 Модуль 7: Orbit Suitability Scorer (Оценщик Пригодности Орбиты)

**Orbit Suitability Scorer** — это гибридный модуль (Математика + Бизнес-логика). Его цель — взять параметры любой космической орбиты (высоту, наклонение) и ответить бизнесу на вопрос: *"Насколько эта орбита подходит для моей коммерческой задачи по 100-балльной шкале?"*

Например:
*   **Задача бизнеса:** Следить за посевами пшеницы в Канаде (Нужно снимать раз в неделю, желательно в одно и то же светлое время суток).
*   **Орбита 1 (МКС, 51°):** Не подходит (Оценка: 30/100). МКС летает над Канадой редко и в разное время суток (то ночью, то днем), освещение на снимках будет разным.
*   **Орбита 2 (Солнечно-синхронная, 98°):** Идеально (Оценка: 95/100). Спутник пролетает над полем всегда ровно в 10:30 утра. Тени одинаковые, фермер доволен.

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Мы должны хранить результаты оценок (скоринга), чтобы пользователь мог сравнивать разные варианты орбит на одном дашборде.

**Задачи по настройке (Supabase):**
*   [ ] Создать таблицу `orbit_scores`:
    *   `id` (Уникальный ID оценки).
    *   `user_id` (Кто запрашивал).
    *   `business_goal_id` (Связь с таблицей целей: "наблюдение", "связь", "погода").
    *   `orbit_parameters_json` (Входные параметры: Высота, Наклонение (i), Эксцентриситет (e)).
    *   `suitability_score` (Итоговый балл: от 0 до 100).
    *   `breakdown_json` (Детализация баллов: Покрытие = 90, Освещенность = 100, Задержка сигнала = 40).
*   [ ] Создать справочник (таблицу `score_weights`), где для каждой бизнес-цели прописаны веса. Например, для шпионского спутника вес "Задержки сигнала (Latency)" = 10% (не так важно), а вес "Разрешения" = 90%. Для интернет-спутника Starlink Задержка Сигнала = 90% (очень важно!).

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI)

Этот модуль (`services/ml-api/app/modules/orbit_scorer.py`) — это калькулятор сложных формул, обернутый в бизнес-логику.

**Задачи по разработке Скорингового Движка:**
*   [ ] **Расчет Покрытия (Coverage Algorithm):** Написать функцию, которая берет координаты Земли (например, широту Канады) и наклонение орбиты. Если наклонение орбиты 30°, а Канада на 50° — спутник *никогда* там не пролетит. Score = 0.
*   [ ] **Расчет Ревизита (Revisit Time):** Используя математические библиотеки (напр. `Skyfield`), рассчитать, как часто спутник на заданной высоте будет пролетать над заданной базой на Земле. Чем чаще — тем выше балл для задач мониторинга.
*   [ ] **Расчет Задержки (Latency):** Простая физика (Расстояние = Высота ऑर्биты). Сигнал летит со скоростью света. Чем выше орбита (например, геостационарная на 36,000 км), тем дольше летит пинг (~600 мс). Если бизнес-задача "Геймерский интернет" — орбита получает Score = 10. Если орбита низкая (LEO, 500 км), то пинг 20мс — Score = 95.
*   [ ] **FastAPI Эндпоинт `POST /api/score-orbit`:** Принимает `business_goal` и `orbit_params`. Вызывает нужные калькуляторы, взвешивает их и возвращает JSON с итоговой оценкой.

---

## 3. 🤖 Нужно ли здесь Машинное Обучение (ML)?

**Нет, классическое ML (на нейросетях) здесь не нужно, и вот почему:**
*   В Машинном обучении (как в Модуле 3 или 5) мы не знаем точной формулы цены или погоды, поэтому просим ИИ её угадать по прошлым данным.
*   В **Модуле 7 (Скорринг)** мы *знаем* все точные формулы. Скорость света постоянна. Формулы прохождения орбиты над широтой постоянны. 

### 🎓 БАЗА: Создание Скоринговой Модели (Scoring Engine)
Вместо ИИ мы строим **Экспертную Систему (Доменный Скоринг)**. Это алгоритмическая модель, основанная на жестких правилах (If-Else) и математике.

**Как работает Скорринг:**
1. Мы определяем ключевые метрики (Датчики).
2. Мы нормируем их (приводим к шкале от 0 до 1).
   * *Пример:* Пинг 0 мс = 1.0 (Идеал). Пинг 1000 мс = 0.0 (Ужас). Пинг 500 мс орбиты = 0.5 балла (Посредственно).
3. Мы накладываем "Бизнес-веса" ($\omega$). 
   * *Задача "Общение фермеров" (Им плевать на пинг):* Вес пинга $w_1 = 0.1$.
   * *Задача "Трейдинг акциями через спутник" (Каждая миллисекунда важна):* Вес пинга $w_1 = 0.9$.
4. **Итоговая формула:** $Score = (Metric_1 * w_1) + (Metric_2 * w_2) + ...$
5. Ответ умножается на 100 и отдается пользователю: **"Оценка вашей орбиты для трейдинга: 42/100"**.

---

## 4. 🌐 Интеграция с Frontend и Gateway

Пользователь должен получить интуитивно понятное сравнение орбит, чтобы принять решение, куда запускать спутник.

*   [ ] **Frontend (Next.js):**
    *   Создать страницу `app/dashboard/suitability-scorer/page.tsx`.
    *   **Шаг 1 (Ввод):** Пользователь выбирает тип бизнеса (из выпадающего списка `Select` в Shadcn) и вписывает параметры орбиты, которую ему предложили инженеры.
    *   **Шаг 2 (Сравнение визуальное):** Фронтенд должен уметь вводить несколько орбит сразу (Орбита А и Орбита Б).
    *   **Шаг 3 (Результат - Радар Чарт):** Идеальный UI для отображения скоринга по разным осям — это Диаграмма-Радар (Radar Chart, паутина в Recharts/Chart.js). 
        *   Пользователь визуально видит "паутину". Орбита А покрывает огромную площадь Земли (паутина вытянута вверх), но у нее ужасная задержка сигнала (паутина провалена вниз). Орбита Б сбалансирована. 
    *   Большая зеленая цифра (например, **88/100**) — как рейтинг в видеоиграх.
*   [ ] **Node.js (Gateway):**
    *   Роут `POST /api/score`, который валидирует данные (чтобы орбита не пересекала Землю, радиус > 0) и отправляет запрос на Python-сервер расчета.

---

**Резюме:** Седьмая функция — это мост между абстрактными физическими числами орбиты и бизнес-требованиями инвестора. Она не использует нейросети, а базируется на взвешенной экспертной системе (Скоринговом движке) и точной физике. Для пользователя всё выглядит как калькулятор рейтинга: он описывает свои цели, а алгоритм выдает понятный балл от 0 до 100 и красивую диаграмму-"радар", детально объясняющую, почему именно эта конфигурация — лучшая (или худшая) инвестиция.
