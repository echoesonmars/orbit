# 🛰 Модуль 6: Orbit Optimizer (Оптимизатор Орбитальных Маневров)

**Orbit Optimizer** — это модуль, кардинально отличающийся от предыдущих. Если раньше мы полагались на статистику и нейросети (учились на прошлом), то здесь вступает в дело **Суровая, точная Физика и Математика (Орбитальная Механика)**.

**Бизнес-задача:** Спутники не могут летать бесконечно — у них ограниченный запас топлива. Топливо — это деньги (сотни тысяч долларов). Чем экономнее спутник меняет орбиту (Маневр Гомана, наклон плоскости), тем дольше он живет и приносит прибыль. Пользователь-инженер должен спросить: *"Как мне дешевле всего переместить спутник с орбиты А на орбиту Б?"*, а наш алгоритм выдаст оптимальную траекторию и затраты топлива (Дельта-V / $\Delta V$).

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Мы не храним огромные обучающие датасеты для этого модуля (так как физика не меняется), но нам нужно хранить результаты расчетов.

**Задачи по настройке (Supabase):**
*   [ ] Создать таблицу `orbit_maneuvers`:
    *   `id` (Уникальный ID расчета).
    *   `user_id` (Кто считал).
    *   `satellite_mass_kg` (Масса аппарата).
    *   `initial_orbit_json` (Апогей, Перигей, Наклонение).
    *   `target_orbit_json` (Целевые параметры).
    *   `delta_v_m_s` (Результат: Требуемое изменение скорости в м/с — главная метрика!).
    *   `fuel_kg` (Результат: Расход топлива).

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI)

Здесь мы не используем `Scikit-learn` или `OpenAI`. Мы переходим на мощные математические и аэрокосмические библиотеки.

*   `services/ml-api/app/modules/orbit_optimizer.py`

**Задачи по разработке математического ядра:**
*   [ ] **Интеграция с `Poliastro` или `Skyfield`:** Это open-source библиотеки на Python для астродинамики. Они умеют решать уравнения Кеплера с феноменальной точностью. Установить в `requirements.txt`.
*   [ ] **Маневр Гомана (Hohmann Transfer):** Написать функцию, которая рассчитывает самый экономный (но долгий) способ перехода между двумя круговыми орбитами.
*   [ ] **Изменение Наклонения (Plane Change):** Написать функцию расчета самого энергозатратного маневра (поворот плоскости орбиты).
*   [ ] **Оценка расхода топлива (Уравнение Циолковского):** Зная, какой нужен импульс ($\Delta V$) из предыдущих пунктов, и зная тип двигателя (Удельный импульс $I_{sp}$), рассчитать, сколько килограмм криптона или ксенона сгорит.
*   [ ] **FastAPI Эндпоинт `POST /api/optimize-orbit`:** Принимает параметры 1 орбиты и 2 орбиты, а возвращает JSON с числами (затраты топлива) и **массивом точек для отрисовки 3D-графика**.

---

## 3. 🤖 Что нужно алгоритмизировать (Здесь НЕТ Machine Learning)

### 🎓 БАЗА: Почему здесь не нужен ML, а нужна Математика?
ИИ (Машинное обучение) ищет *приближенные* вероятности. Если ИИ предскажет цену снимка с ошибкой в $5 — ничего страшного. 
Но если ИИ предскажет силу импульса ракетного двигателя с ошибкой в 5 метров в секунду — спутник улетит в открытый космос или сгорит в атмосфере. Физика требует абсолютной точности (Аналитических решений).

**Что такое $\Delta V$ (Дельта-Вэ)?**
Это главная валюта в космосе. Это то, насколько сильно нужно "пнуть" спутник, чтобы изменить его скорость и орбиту. Запас $\Delta V$ закладывается при сборке.
Если мы знаем, что нам нужно перейти с орбиты высотой 400 км на 600 км, мы используем не ChatGPT, а формулы Исаака Ньютона и Иоганна Кеплера. 

**Пример логики (Внутри Python):**
1. Пользователь передает: Орбита 1 (Радиус $r_1$), Орбита 2 (Радиус $r_2$).
2. Наша Python-библиотека (`Poliastro`) строит эллипс между ними.
3. Библиотека считает скорость "пинка" в первой точке (Перигей) и скорость торможения во второй (Апогей).
4. Сумма этих ускорений = $\Delta V$.
5. Мы множим $\Delta V$ на массу спутника и получаем расход топлива в $$$. Платформа выдает инвестору ответ: *"Этот маневр обойдется вам в $45,000 сгоревшего топлива"*.

*Опционально:* Если клиент запрашивает сложный маршрут через 5 орбит (Обслуживание группировки спутников), мы можем применить **Генетические Алгоритмы (Genetic Algorithms / SciPy Optimize)** для поиска *наилучшего маршрута*, похожего на задачу коммивояжера.

---

## 4. 🌐 Интеграция с Frontend и Gateway

Математические данные скучны, пока их не визуализируешь. Главная фишка этого модуля для фронтенда — **Красивая 3D-Карта (WebGL)**.

*   [ ] **Frontend (Next.js):**
    *   Страница `app/dashboard/optimizer/page.tsx`.
    *   Левая колонка: Формы ввода (Ползунки). Высота орбиты А (км), Высота орбиты Б (км), Масса спутника (кг).
    *   **Интеграция 3D (CesiumJS или Three.js):** Вместо плоской карты 2D Google Maps, здесь должна крутиться красивая 3D-модель Земли в темном космосе. 
    *   Когда Python присылает рассчитанный массив точек маневра, фронтенд (Three.js) рисует красивую эллиптическую светящуюся линию между двумя орбитами, показывая траекторию полета.
*   [ ] **Node.js (Gateway):**
    *   Принимает параметры ползунков, валидирует, что пользователь не ввел отрицательную массу (Zod: `z.number().positive()`) и шлет запрос в Python.

---

**Резюме:** Шестая функция — это дань классической инженерии и аэрокосмическим расчетам (Astrodynamics). Она не угадывает вероятности, она считает физику. Код пишется с использованием тяжелых физических библиотек (`Poliastro`). Но для пользователя OrbitAI это выглядит как магия: он просто дергает ползунки на сайте, а система на фоне решает дифференциальные уравнения и крутит красивый 3D-глобус с орбитами.
