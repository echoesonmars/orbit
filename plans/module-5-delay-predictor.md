# 🚀 Модуль 5: Launch Delay Predictor (Предсказатель Задержек Запуска)

**Launch Delay Predictor** — это аналитический модуль, который оценивает вероятность того, что запланированный запуск ракеты-носителя (например, Falcon 9 или Electron) будет отложен.

Для бизнеса, который платит миллионы за вывод своего спутника на орбиту, каждый день задержки на космодроме означает колоссальные убытки. Если ИИ скажет: *"У вас 85% вероятность переноса запуска из-за погоды во Флориде"*, компания сможет заранее перенести маркетинговую кампанию или перенастроить оборудование.

С точки зрения ИИ — это задача **Предсказания по Временным Рядам (Time-Series Forecasting)** и **Бинарной Классификации** (Отложат / Не отложат).

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Нам нужна историческая база всех запусков ракет за последние годы (состоявшихся и отмененных).

**Задачи по настройке (Supabase):**
*   [ ] Создать таблицу `launch_history`:
    *   `id` (Уникальный ID запуска).
    *   `rocket_type` (Falcon 9, Soyuz, Electron).
    *   `spaceport` (Канаверал, Байконур, Куру).
    *   `scheduled_date` (Когда планировали).
    *   `actual_date` (Когда реально полетели).
    *   `delay_days` (Разница в днях. Если 0 — улетели вовремя).
    *   `weather_conditions_json` (Погода в день `scheduled_date`).
    *   `reason` (Причина отмены: Погода, Техника, Скраб).

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI)

Этот модуль (`services/ml-api/app/modules/delay_predictor.py`) будет собирать данные из внешних API космодромов и погоды, чтобы применять обученную модель к будущим запускам.

**Задачи по разработке Data Pipeline:**
*   [ ] **Интеграция с Launch Library 2 (The Space Devs API):** Написать скрипт, который стягивает расписание будущих запусков (`Upcoming Launches`). Это публичное API, где есть даты, космодромы и ракеты.
*   [ ] **Интеграция с Метео-API (Open-Meteo):** Запуски ракет безумно зависят от погоды (скорость ветра на высоте 10 км, грозы). Скрипт должен по координатам космодрома и дате будущего запуска скачивать почасовой прогноз погоды.
*   [ ] **FastAPI Эндпоинты:**
    *   `GET /api/launches/upcoming` — Возвращает список будущих запусков для отображения на фронтенде в виде карточек.
    *   `POST /api/predict-delay` — Принимает ID запуска, собирает свежую погоду, прогоняет через ML-модель и возвращает вероятность задержки.

---

## 3. 🤖 Что нужно обучать (ML & Data Science)

В отличие от Модуля 3 (где мы предсказывали цену — Регрессия), здесь мы решаем задачу **Классификации** (Вероятность события от 0% до 100%).

### 🎓 БАЗА: Как ИИ учится предсказывать задержки (Классификация)

**Шаг 1: Подготовка Исторического Датасета**
Мы скачиваем таблицу всех запусков SpaceX, Rocket Lab и ULA за последние 10 лет.
*   *Фича 1:* Тип ракеты (Falcon 9 летает часто и надежно, а новые ракеты вроде Starship — 100% задерживаются).
*   *Фича 2:* Космодром (Во Флориде летом сезон ураганов, запуски часто отменяют. В Казахстане климат другой).
*   *Фича 3:* Скорость ветра на высотах (Сдвиг ветра — частая причина отмены).
*   *Фича 4:* Окно запуска (Мгновенное окно для полета к МКС сложнее, чем 4-часовое окно для раздачи Starlink).
*   **Target (Ответ для ИИ):** `1` (Полет состоялся вовремя), `0` (Запуск отменили).

**Шаг 2: Выбор Алгоритма**
Для классификации табличных данных с сильной зависимостью от категорий (Название ракеты, Космодром) мы снова используем **CatBoost Classifier** или **XGBoost Classifier**.
Но здесь важна еще одна вещь: **Time-Series (Временные ряды)**. Модель должна понимать, что если месяц назад взорвалась ракета Falcon 9, то все следующие запуски Falcon 9 будут отложены на месяцы для расследования (эффект домино).

**Шаг 3: Обучение и Сохранение Весов**
Скармливаем эти 5,000 исторических запусков алгоритму.
```python
from catboost import CatBoostClassifier

# Обучаем модель классификации
model = CatBoostClassifier(iterations=1000, learning_rate=0.05, depth=6)
model.fit(X_train, y_train, cat_features=["rocket_type", "spaceport"])

# Модель научилась! Сохраняем её для сервера.
model.save_model("delay_classifier.cbm")
```

**Шаг 4: Inference (Прогноз будущего)**
Когда юзер открывает карточку миссии на сайте, Python-сервер смотрит прогноз погоды на завтра на мысе Канаверал, смотрит статистику ракеты и спрашивает модель.
Модель отвечает не просто "Отложат", а отдает **вероятность (Probability)**: `predict_proba() -> [0.85, 0.15]`.
Значит: 85% шанс, что ракету перенесут (например, из-за ветра), и 15%, что она улетит вовремя.

---

## 4. 🌐 Интеграция с Frontend и Gateway

Это очень "кликабельная" и визуально красивая функция для дашборда.

*   [ ] **Frontend (Next.js):**
    *   Создать страницу `app/dashboard/launches/page.tsx`.
    *   Реализовать интерфейс в виде "Списка Рейсов" в аэропорту (Таблица предстоящих запусков: Дата, Ракета, Груз).
    *   При клике на строку открывается модальное окно (`Dialog` из Shadcn UI) с деталями прогноза.
    *   Внутри окна красивый спидометр (Gauge Chart) от 0 до 100%, показывающий **Риск Задержки (Delay Risk)**, подсвеченный красным, если риск > 50%.
    *   Ниже отображается карточка погоды (Ветер, Облака), стянутая с метео-API.
*   [ ] **Node.js (Gateway):**
    *   Прослойка-кэш: Так как расписание запусков не меняется каждую секунду, Node.js должен кэшировать ответ от Python-сервиса (в Redis), чтобы не дергать базу данных на каждый клик юзера. Время жизни кэша (TTL) — 1 час.

---

**Резюме:** Пятая функция показывает бизнесу не цены, а **время и риски**. Это классическая задача предсказания вероятностей (Бинарной Классификации). Главная сложность здесь не в самой ML-модели (алгоритм CatBoost легко справится), а в **интеграциях**: нам нужно постоянно собирать актуальные расписания запусков из открытых API и скрещивать их с тонкими метеорологическими прогнозами. Только так ИИ сможет точно предугадывать переносы стартов.
