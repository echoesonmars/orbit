# 🌐 Модуль 1: Data Hub (Ингресс и хранение геоданных)

**Data Hub** — это фундамент платформы OrbitAI. Без качественных пространственных данных (спутниковых снимков, телеметрии, орбит) ни одна ML-модель или аналитический алгоритм работать не будет. 

Цель этого модуля — научиться автоматически скачивать, обрабатывать (нормализовать) и сохранять гео-данные в нашей базе данных (Supabase PostGIS), чтобы другие ИИ-модули платформы могли обращаться к ним за секунды, а не скачивать заново из интернета каждый раз.

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Так как мы работаем со спутниковыми снимками и географическими зонами (BBox - Bounding Box), нам нужна мощная гео-пространственная база данных. Мы используем **PostgreSQL + расширение PostGIS** внутри Supabase.

**Задачи по настройке:**
*   [ ] Подключиться к Supabase и выполнить SQL-скрипт `CREATE EXTENSION IF NOT EXISTS postgis;`
*   [ ] Создать таблицу `satellite_telemetry` для хранения орбит (TLE данных) аппаратов.
*   [ ] Создать таблицу `earth_observations` для метаданных о спутниковых снимках (где сняли, когда, какая облачность, какой спутник). Важно: сами терабайты снимков мы не храним, мы храним ссылки (URL на AWS S3) и `geometry` (полигон съемки).
*   [ ] Настроить пространственные индексы (GiST Index), чтобы поиск ответа на вопрос "какие снимки покрывают эту ферму в Бразилии" занимал миллисекунды.

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI)

Python-ядро (в папке `services/ml-api/app/modules/geo_processor.py`) будет выполнять всю черновую работу по извлечению данных.

**Задачи по разработке скриптов (Ingestion Pipelines):**
*   [ ] **Интеграция с Celestrak/Space-Track (TLE):** Написать функцию, которая раз в сутки стучится в публичные APIs, скачивает TLE-файлы (параметры орбит тысяч спутников) и обновляет нашу БД.
*   [ ] **Интеграция с Sentinel Hub / STAC API:** Написать клиент для поиска метаданных об открытых спутниковых снимках (Sentinel-2, Landsat). Пример: функция `fetch_images_metadata(bbox, start_date, end_date)`.
*   [ ] **Парсинг и Нормализация:** Написать класс с использованием библиотеки `GeoPandas`, который конвертирует чужие гео-форматы (GeoJSON, WKT) в единый стандарт платформы перед записью в нашу базу.
*   [ ] **API Эндпоинты в FastAPI:** Создать роуты, такие как `GET /data/search-images?bbox=...&cloud_cover=<10` — чтобы Node.js шлюз мог легко запрашивать эти данные для фронтенда.

---

## 3. 🤖 Что нужно обучать (ML & Data Science)

Хотя сам по себе Data Hub не является предсказательной моделью, он отвечает за "Feature Engineering" (подготовку признаков) для будущих ИИ-модулей. Без правильно настроенного пайплайна здесь мы не сможем обучать модели дальше.

**Задачи по Data Science:**
*   [ ] **Очистка данных (Data Cleaning):** Написать алгоритмы отбраковки битых метаданных (например, когда спутник записал координаты с ошибкой оборудования).
*   [ ] **Генерация фичей (Feature Selection):** Из сырых снимков извлечь "чистые" признаки. Например, скрипт должен автоматически рассчитывать индекс NDVI (вегетационный индекс) на основе инфракрасного канала снимка и складывать это числовых значение в БД, чтобы потом модель `Capture Value Predictor` (Предсказатель ценности снимка) могла использовать эту цифру для оценки того, купят ли этот снимок фермеры.

---

## 4. 🌐 Интеграция с Frontend и Gateway

Пользователь должен визуально взаимодействовать с Data Hub на дашборде.

*   [ ] **Node.js (Gateway):** Написать контроллер, который принимает BBox с фронтенда, валидирует его (Zod) и передает в Python.
*   [ ] **Frontend (Next.js):** 
    *   Сделать так, чтобы при выделении квадрата на карте пользователем, отправлялся API запрос.
    *   Реализовать отображение найденных снимков (метаданных) в боковой панели (Sidebar) с анимацией загрузки.

---

## 🎓 БАЗА: Как работает Машинное Обучение (ML) в нашем проекте простыми словами

Если вы совсем новичок в ML, представьте, что **Машинное Обучение (Machine Learning)** — это как обучение маленького ребенка определять, дорогие это кроссовки или дешевые. Вы не пишете ему строгие правила ("если подошва 2 см, то это дорого"), а просто показываете 1000 фотографий кроссовок и говорите: "Вот эти стоят $50, а эти $500". Ребенок (ИИ) сам находит закономерности (бренд, материал, форма), о которых вы даже не думали.

В нашем проекте (OrbitAI) всё работает точно так же, только вместо кроссовок у нас — **Космические снимки**.

### 📌 Шаг 1: Сбор данных (То, что делает Data Hub)
Чтобы научить ИИ оценивать коммерческую ценность будущего снимка (например, "сколько фермеры заплатят за свежий снимок этого поля"), нам нужен "учебник" для ИИ — исторические данные. 

**Где брать эти данные бесплатно?**
1.  **Sentinel Hub (Европейское космическое агентство) / USGS (Геологическая служба США):** Это открытые базы данных. Там лежат миллионы реальных снимков Земли (Landsat, Sentinel-2). 
2.  **API погоды (OpenWeatherMap / NASA POWER):** Спутники не видят сквозь тучи (в оптическом диапазоне). Нам нужны данные о погоде в момент снимка.
3.  **Space-Track / CelesTrak:** Базы данных орбит. Где летел спутник в тот день?

### 📌 Шаг 2: Подготовка "Учебника" для ИИ
ИИ не ест "картинки". ИИ ест **цифры** в виде таблицы (Excel). Нам нужно извлечь из сырых данных колонки (Они называются **Признаками - Features**). 

Наша таблица для обучения ИИ будет выглядеть так:
*   *Колонка 1 (X1):* Процент облачности (например, 15%).
*   *Колонка 2 (X2):* Тип местности (Лес = 1, Город = 2, Поле = 3).
*   *Колонка 3 (X3):* Индекс растительности (NDVI - насколько зеленые растения сейчас там).
*   *Колонка 4 (X4):* Месяц года (Август — время сбора урожая, спрос огромный!).
*   **Колонка-ответ (Target / Y):** Ценность снимка. *Это то, что ИИ должен отгадывать! У нас будут исторические данные о том, за сколько такие снимки покупали на биржах вроде SkyWatch.*

Всё это делает Python-скрипт. Он парсит интернет, собирает эти факторы и складывает в базу, аккуратными строчками.

### 📌 Шаг 3: Само Обучение (Тренировка)
Когда у нас есть таблица на 10,000 строк из прошлых снимков, мы скармливаем её алгоритму. В нашем случае это будет алгоритм из библиотеки **Scikit-learn** или **LightGBM** (очень быстрый алгоритм, который отлично работает с таблицами).

**Как выглядит код в Python (Упрощенно):**
```python
from lightgbm import LGBMRegressor

# X - это наши признаки (облачность, месяц, зелень)
# y - это цена (то, что мы хотим научиться угадывать)
model = LGBMRegressor()

# Команда "УЧИСЬ!" (ИИ смотрит на 10 000 примеров и ищет зависимости)
model.fit(X_train, y_train)

# Сохраняем "мозг" (наш обученный файл) на диск
import joblib
joblib.dump(model, 'value_predictor.pkl')
```

### 📌 Шаг 4: Использование ИИ (Inference)
Теперь приходит наш клиент-фермер на сайт **(Frontend на Next.js)**. Он рисует квадрат на карте и говорит: *"Я хочу заказать снимок через 3 дня"*. 
Фронтенд отправляет координаты на бэкенд (Gateway -> ML API).

Бэкенд делает магию:
1. Загружает наш сохраненный "мозг" (`value_predictor.pkl`).
2. Смотрит прогноз погоды на эти 3 дня (например, будет 40% облачности).
3. Спрашивает ИИ: *"Эй, у нас тут поле, 40% облаков, месяц август. Сколько это может стоить?"*
4. Алгоритм моментально прогоняет эти цифры через свои нейроны/веса, которые выучил на Шаге 3, и отвечает: *"Вероятная ценность снимка: $2,500. Уценка из-за облаков 10%!"*.
5. Эта цифра красиво рисуется пользователю на экране.

---
**Итог:** Всё начинается со сбора "сырья" (Данных) из открытых источников. Затем Python пережевывает их в цифры. Затем ML-модель учится искать в этих цифрах закономерности. А затем мы просто спрашиваем её будущее. Data Hub (о котором этот документ) отвечает за самое главное — **за качественный сбор сырья**.

