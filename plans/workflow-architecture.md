# 🛰 OrbitAI: Полная Архитектура и Workflow 10 Модулей (Master Spec)

Этот документ описывает полный жизненный цикл данных (Data Flow) для каждого из 10 узловых модулей платформы OrbitAI. Он связывает воедино Frontend (Next.js), API Gateway (Node.js/Express) и ML/Python микросервисы на основе технической документации.

---

## 🏗 Общая Инфраструктура
- **Web Frontend:** Next.js 14, Tailwind CSS, Shadcn UI, Zustand, Leaflet (Карты), Recharts/Three.js (Графики/3D).
- **API Gateway:** Node.js (Express), валидация Zod. Валидирует JWT токены от Supabase.
- **Microservices:** Python (FastAPI) для ML и вычислений. Celery/Redis для фоновых задач.
- **Database:** Supabase (PostgreSQL + PostGIS для геометрии). S3 Storage для файлов.

---

## 📦 Модуль 1: Data Hub (Ингресс и хранение геоданных)
**Задача:** Автоматический сбор гео-данных (снимков, орбит) для обучения и использования остальными модулями.
1. **Cron/Worker (Python):** Раз в сутки качает параметры орбит (TLE) из Space-Track и метаданные снимков из Sentinel Hub.
2. **Обработка:** Нормализация геометрии через `GeoPandas`.
3. **Хранение:** Запись абстрактных полигонов в PostGIS (`earth_observations` и `satellite_telemetry`).
4. **Frontend Workflow:** Пользователь на `InteractiveMap` рисует BBox -> Gateway делает запрос `GET /data/search-images?bbox=...` -> Быстрый пространственный поиск (`ST_Intersects`) в PostGIS возвращает снимки.

## 🤖 Модуль 2: Mission Designer (Генератор ТЗ)
**Задача:** Перевод хотелок бизнеса в строгий JSON аэрокосмического задания.
1. **Frontend:** Чат-интерфейс. Юзер пишет: *"Следить за портом Роттердам раз в 3 дня"*.
2. **Gateway:** `POST /api/design-mission` (передача промпта).
3. **ML Service (LLM / Prompt Engineering):** Вызов OpenAI/Anthropic API с системным промптом "Инженера-баллистика". Превращение текста в `MissionSpec` (Орбита, GSD, Сенсор).
4. **Database:** Сохранение результата в таблицу `mission_specs`.
5. **UI Update:** Отрисовка сгенерированного ТЗ карточками параметров.

## 💎 Модуль 3: Capture Value Predictor (Предсказание цены)
**Задача:** Регрессионный ML для оценки коммерческой стоимости будущего снимка.
1. **Frontend:** На карте (Leaflet) выделяется зона -> `POST /api/predict-value`.
2. **Backend (Python):** 
   - Стягивает погоду (OpenWeatherMap) для BBox.
   - Загружает веса ML-модели (CatBoost/LGBMRegressor) из RAM.
   - Скармливает фичи (Облачность, Тип местности, Месяц) в модель.
3. **Response:** Итог (Цена $1250, Вероятность 88%).
4. **Хранение:** Логирование в `predictions_history` для аналитики "Хот-спотов".

## 📄 Модуль 4: Report Generator (Генератор отчетов)
**Задача:** Explainable AI (XAI) и генерация PDF для бизнеса.
1. **Frontend:** Кнопка "Скачать PDF". Начинается поллинг (Polling) статуса.
2. **Gateway:** `POST /api/reports/generate`. Кидает ID задачи в **Redis Queue** и сразу отвечает `202 Accepted`.
3. **Worker (Python):** 
   - Запускает SHAP/LIME для объяснения предсказаний ML (почему цена $1000?).
   - Рендерит графики (Plotly), скачивает карту (Mapbox Static API).
   - `Jinja2` + `WeasyPrint` собирают HTML в PDF.
4. **Хранение:** PDF летит в Supabase Storage (бакет `mission_reports`).
5. **Frontend:** Получает `status: completed` и открывает прямую ссылку на скачивание.

## 🚀 Модуль 5: Launch Delay Predictor (Задержки запуска)
**Задача:** Бинарная ML классификация риска переноса пуска ракеты.
1. **Data Pipeline:** Python стягивает расписание запусков (The Space Devs API) и кэширует в Redis.
2. **Frontend:** Юзер кликает на карточку будущего пуска Falcon 9.
3. **ML Inference:** CatBoost Classifier, обученный на исторических данных (космодром, ветер, тип ракеты), оценивает вероятность задержки.
4. **UI Response:** Спидометр (Gauge Chart) показывает риск отмены (например, 85%) и карточку прогноза погоды.

## 🛰 Модуль 6: Orbit Optimizer (Оптимизатор орбит)
**Задача:** Точные математические расчеты орбитальных маневров (без ИИ).
1. **Frontend:** Юзер двигает ползунки масс и орбит А/Б на 3D-карте (Three.js/Cesium).
2. **Gateway:** `POST /api/optimize-orbit`.
3. **Python (Астродинамика):** Используются библиотеки `Poliastro` / `Skyfield`. Идут точные вычисления уравнения Циолковского, маневра Гомана.
4. **Response:** Значение Дельта-V ($\Delta V$), затраты топлива (кг) и массив точек 3D-траектории (Vector data) для отрисовки на фронтенде плавной линии перелета.

## 🎯 Модуль 7: Orbit Suitability Scorer (Оценщик орбит)
**Задача:** Экспертная система (Rule-based) скоринга орбит под бизнес-цели (0-100 баллов).
1. **Frontend:** Юзер выбирает "Шпионский спутник" и параметры (ССС-орбита 500 км).
2. **Scoring Engine (Python):** 
   - Считает пинг (Latency), зону покрытия (Coverage), частоту пролетов (Revisit).
   - Нормирует метрики от 0 до 1 и умножает на веса бизнес-цели ($w_i$).
3. **UI Response:** Большая цифра "95/100" и диаграмма-радар (Radar Chart) в Recharts, показывающая баланс орбиты по разным осям.

## 🕵️‍♂️ Модуль 8: Failure Forensics (Расследование аномалий)
**Задача:** Unsupervised ML для поиска сбоев в телеметрии спутников.
1. **Frontend:** Drag & Drop загрузка гигантского CSV (или JSON) лога телеметрии.
2. **Gateway:** Стриминг файла (`multer`) в Python.
3. **ML Service:** 
   - Интерполяция пропусков датчиков (Pandas).
   - Запуск алгоритма поиска аномалий **Isolation Forest** (или Autoencoders).
4. **Response:** Массив timestamps с ошибками. Фронтенд рисует интерактивный `ApexChart`, подсвечивая красными точками (аномалиями) синюю линию графика. Дополнительно LLM генерирует текст "вероятная причина: замыкание батареи".

## 🎲 Модуль 9: Scenario Simulator (Монте-Карло симулятор)
**Задача:** Оценка бизнес-рисков (ROI) через стохастическое моделирование.
1. **Frontend:** Ползунки рисков (Вероятность взрыва, Цена пуска, Цена связи). Запуск симуляции.
2. **Worker (Python/Redis):** 
   - Библиотека SciPy/NumPy прогоняет 10,000 жизненных циклов спутника (Метод Монте-Карло), подкидывая виртуальные кубики на каждое событие.
3. **Response:** Набор квантилей (P10, P50, P90). Фронтенд отрисовывает Конус Неопределенности (Fan Chart), показывая медианную окупаемость 3.2 года и риск банкротства.

## 🌍 Модуль 10: ESG Assessor (Экологический рейтинг)
**Задача:** Life Cycle Assessment (LCA) выбросов $CO_2$ и расчет риска мусора.
1. **Frontend:** Форма массы аппарата, типа топлива (Керосин/Метан) и наличия паруса/двигателя свода.
2. **Calculator (Python):**
   - Умножение массы топлива на Emissions Factors (таблица БД).
   - Логика орбиты: если высота >600 км без двигателя свода, ставится "Grade F" (Космический мусор).
3. **Generative UI:** LLM-модуль объясняет оценку текстом и дает советы: "Установите магнитный парус для быстрого сгорания". Фронтенд показывает Donut Chart с буквами (A, B, C, D).
