# 🧠 Модуль 2: Mission Designer (Генератор Технических Заданий)

**Mission Designer** (Проектировщик Миссий) — это вторая ключевая функция нашей платформы. Её задача: переводить простые желания бизнесмена на сложный язык космической инженерии.

Например:
*   **Клиент пишет:** *"Я владелец сети супермаркетов в Сибири. Мне нужно знать, сколько машин паркуется у магазинов конкурентов каждый день."*
*   **Mission Designer выдает инженеру:** *"Орбита: Солнечно-синхронная, Угол съемки: < 15°, Режим сенсора: Оптический (RGB), Разрешение (GSD): < 0.5 метра, Частота посещения (Revisit time): 1 раз в сутки (в 12:00 местного времени)."*

В отличие от первого модуля (где мы оперируем строгими таблицами и цифрами), здесь мы работаем со **свободным текстом**. 

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Мы должны сохранять историю того, что просил пользователь и что сгенерировал ИИ.

**Задачи по настройке БД (Supabase):**
*   [ ] Создать таблицу `mission_prompts` (Id миссии, Id пользователя, Исходный текст клиента).
*   [ ] Создать таблицу `mission_specs` (Техническое задание: Орбита, Разрешение, Режим камеры). Это будет храниться в формате JSON, чтобы было удобно выводить на фронтенде.

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI)

Здесь нам не нужно писать сложные математические формулы. Мы будем использовать готовые нейросети, которые умеют понимать текст на человеческом языке (Large Language Models или **LLM**).

*   `services/ml-api/app/modules/mission_designer.py`

**Задачи по разработке:**
*   [ ] **Интеграция с OpenAI API:** Зарегистрировать аккаунт в OpenAI (разработчики ChatGPT) или Anthropic (Claude), получить секретный API-ключ и добавить его в файл `.env`.
*   [ ] **Написать Pydantic-схему (Контракт):** Принудить ИИ всегда отвечать строго в заданном формате (JSON), а не писать сочинения.
    ```python
    class MissionSpec(BaseModel):
        orbit_type: str        # 'Sun-synchronous', 'Geostationary'...
        resolution_meters: float # 0.5, 10.0
        sensor_type: str       # 'Optical', 'SAR'
        explanation: str       # 'Почему мы выбрали именно это?'
    ```
*   [ ] **Создать FastAPI Эндпоинт:** `POST /api/design-mission` — будет принимать текст с фронтенда, отправлять скрытый запрос в OpenAI и возвращать готовое JSON техническое задание.

---

## 3. 🤖 Что нужно обучать (ML & Data Science)

**Хорошая новость для новичка:** в этом модуле нам **НЕ НУЖНО** обучать модель с нуля! Обучение языковой модели типа GPT стоит миллионы долларов. Мы будем использовать другой навык ML-инженера — **Prompt Engineering (Промпт Инжиниринг)**.

### 🎓 БАЗА: Что такое NLP и Промпт Инжиниринг простыми словами?

Представьте, что у вас есть невероятно умный, но абсолютно потерянный ассистент с энциклопедическими знаниями (ChatGPT). Если вы просто спросите его: *"Сделай миссию для парковок"*, он выдаст 5 страниц бесполезной воды про парковки в целом.

**Промпт Инжиниринг** — это искусство составить идеальную словесную инструкцию (Prompt), которую мы (незаметно от пользователя) приклеиваем к его запросу перед отправкой в ИИ. Мы "вкладываем" знания в контекст модели.

**Как мы настроим ИИ (Системный Промпт):**
В коде Python мы пропишем жесткую инструкцию для API:

> *"Ты — Senior Космический Инженер-Баллистик с 20-летним стажем. Пользователь пришлет тебе бизнес-задачу.*
> *Правило 1: Если бизнесу нужно считать машины, тебе нужен оптический сенсор очень высокого разрешения (<0.5м).*
> *Правило 2: Если бизнесу нужно следить за кораблями ночью или в облачную погоду, оптический сенсор не сработает! Назначай сенсор спутника типа SAR (Радар с синтезированной апертурой).*
> *Твоя задача — проанализировать текст пользователя и вернуть строго JSON-ответ без лишних слов."*

**Поток работы (Inference):**
1.  **Действие:** Пользователь пишет на сайте: *"Хочу следить за движением судов в порту Лондона зимой."*
2.  **Склейка (Под капотом):** Наш Python-код берет этот текст, приклеивает к "Системному Промпту" (с правилом про облачность и радары) и отправляет в OpenAI.
3.  **Ответ ИИ:** OpenAI понимает: *"Ага, Лондон зимой — это туманы и облака. Оптика не пробьет облака. Выбираю радиолокационный спутник (SAR)."* И возвращает нам технический JSON.

---

## 4. 🌐 Интеграция с Frontend и Gateway

Пользователю должно казаться, что в системе сидит живой инженер.

*   [ ] **Frontend (Next.js):** 
    *   Сделать страницу `app/dashboard/mission-designer/page.tsx`.
    *   Создать UI, похожий на чат или современную форму.
    *   Когда приходит ответ (JSON), красиво отрисовать его карточками (например: Карточка "Сенсор" с иконкой радара, Карточка "Орбита" с иконкой Земли).
*   [ ] **Node.js (Gateway):** Контроллер, который принимает текстовую строку от пользователя, обрезает слишком длинные сообщения (защита от взлома и спама), проверяет JWT-токен (чтобы бесплатные пользователи не жгли наши деньги в OpenAI) и отправляет запрос в Python.

---

**Резюме:** Вторая функция (Mission Designer) — это мост между обывателем и инженером. Она работает на базе обработки естественного языка (NLP) через сторонние API (например, OpenAI). Ключевая работа здесь — это не обучение математических моделей по таблицам (как в Data Hub), а написание идеальных "промптов" (инструкций) и создание надежного API, которое заставляет ИИ выдавать четкие, технические JSON-данные вместо пустой болтовни.
