# 🕵️‍♂️ Модуль 8: Failure Forensics (Аналитика Аномалий и Поломок)

**Failure Forensics** — это ИИ-детектив платформы OrbitAI. 
Спутник в космосе нельзя починить руками. Если он внезапно перестал отвечать или начал вращаться (Tumbling), у инженеров на Земле есть только один источник информации — **Телеметрия** (сотни графиков температуры, напряжения, датчиков солнца).

Задача этого модуля: проанализировать мегабайты скучных графиков телеметрии и с помощью Машинного Обучения (ML) найти ту самую миллисекунду, когда температура батареи №3 подозрительно подскочила на 0.5 градуса перед полным отказом спутника.

Это классическая задача **Поиска Аномалий во временных рядах (Anomaly Detection)**.

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Телеметрия — это огромный поток данных (Time-Series Data). Обычный PostgreSQL с ним справляется плохо. В идеальном мире для этого используют базы данных вроде InfluxDB или TimescaleDB. Но так как мы используем Supabase, мы задействуем мощь JSONB.

**Задачи по настройке БД (Supabase):**
*   [ ] Создать таблицу `telemetry_logs`:
    *   `id` (Уникальный ID).
    *   `satellite_id` (Какой спутник).
    *   `timestamp` (Точное время до миллисекунд).
    *   `sensors_data` (JSONB: `{ "temp_battery": 24.5, "voltage_bus": 28.1, "reaction_wheel_rpm": 3000 }`).
*   [ ] Создать таблицу `detected_anomalies`:
    *   `telemetry_id` (Ссылка на строку, где ИИ нашел проблему).
    *   `severity` (Опасность: Warning, Critical, Fatal).
    *   `description` (Текстовое объяснение ИИ: "Резкое падение напряжения шины").

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI)

Этот модуль (`services/ml-api/app/modules/forensics.py`) должен уметь принимать огромные CSV-файлы сырой телеметрии от пользователя и прогонять их через ML-фильтры.

**Задачи по разработке API:**
*   [ ] **FastAPI Эндпоинт `POST /api/forensics/analyze`:** Должен принимать загрузку файла (UploadFile). Пользователь кидает CSV с логами за месяц работы спутника.
*   [ ] **Data Preprocessing (Pandas):** Написать функцию, которая чистит CSV. Часто датчики присылают `NaN` (пустоту) из-за помех связи. Пандас должен интерполировать эти пропуски, чтобы алгоритм не сломался.
*   [ ] **Inference (Запуск ML):** Прогнать очищенную таблицу через обученную ML-модель поиска аномалий.
*   [ ] **Генератор Ответов:** Вернуть на фронтенд JSON, в котором указаны конкретные временные метки (`timestamps`), где ML нашел странное поведение.

---

## 3. 🤖 Что нужно обучать (ML & Data Science)

В отличие от Модуля 3 (где мы предсказывали цену), здесь мы используем **Обучение без учителя (Unsupervised Learning)**. 
Почему? Потому что поломки спутников уникальны. У нас нет базы из 10,000 одинаковых сломанных батарей, чтобы научить ИИ говорить "Вот это поломка батареи". ИИ должен сам понять, что такое "Норма", и кричать, когда видит отклонение.

### 🎓 БАЗА: Как ИИ ищет 아номалии (Isolation Forest / Autoencoders)

**Шаг 1: Датасет "Нормы"**
Мы загружаем в ИИ телеметрию спутника за тот месяц, когда он работал **идеально**. 

**Шаг 2: Выбор Алгоритма (Isolation Forest)**
Алгоритм "Изолирующий Лес" (Isolation Forest из библиотеки `scikit-learn`) работает гениально просто:
Он строит деревья решений. Если точка данных (например, температура 50 градусов) сильно отличается от других (обычная температура 20 градусов), алгоритму очень легко "отрезать" её от остального леса буквально за 1-2 вопроса. Чем быстрее точка изолируется, тем выше её "Счет Аномальности" (Anomaly Score).

*Альтернатива (Продвинутая):* **Автоэнкодеры (Нейросети PyTorch/Keras)**. 
Они учатся сжимать данные "Нормы" и разжимать их обратно. Если мы подадим им телеметрию в момент поломки, нейросеть не сможет её правильно сжать/разжать (потому что она никогда такого не видела в "Норме"). Возникнет большая ошибка восстановления (Reconstruction Error). Эта ошибка и есть сигнал тревоги (Аномалия!).

**Шаг 3: Обучение и Сохранение**
```python
from sklearn.ensemble import IsolationForest

# Учим ИИ понимать, как выглядит "Здоровый" спутник (df_normal)
model = IsolationForest(contamination=0.01) # Говорим, что нормальных аномалий < 1%
model.fit(df_normal)

import joblib
joblib.dump(model, 'anomaly_detector.pkl')
```

---

## 4. 🌐 Интеграция с Frontend и Gateway

Пользователю (инженеру) нужен сложный, профессиональный интерфейс для работы с графиками.

*   [ ] **Frontend (Next.js):**
    *   Создать страницу `app/dashboard/forensics/page.tsx`.
    *   Зона `Drag & Drop` (DropzoneReact) для загрузки тяжелых CSV файлов (до 50 Мб).
    *   **Интерактивные Графики (Time-Series Charts):** Использовать библиотеку `ApexCharts` или `Plotly.js`. На экране рисуется линия напряжения за месяц.
    *   **Подсветка Аномалий:** Те миллисекунды (Timestamp), которые Python-сервер пометил как аномалии, должны подсвечиваться на графике ярко-красным цветом (Красные точки поверх синей линии телеметрии).
    *   **Панель Инсайтов:** Сбоку выводится сгенерированный (через OpenAI / Модуль 2) человеческий текст: *"Аномалия №1: Напряжение упало на 15% за 2 секунды. Возможная причина: короткое замыкание солнечной панели."*
*   [ ] **Node.js (Gateway):**
    *   Настроить `multer` (или аналог) для безопасного приема CSV файлов от клиента и потоковой передачи (Streaming) огромного файла в Python-сервис, чтобы не забить оперативную память шлюза.

---

**Резюме:** Восьмая функция — это мощнейший инструмент глубокой технической аналитики. Она использует алгоритмы Машинного Обучения "без учителя" (Unsupervised ML, например, Isolation Forest), чтобы автоматически просеивать горы скучных логов и находить иголку в стоге сена — ту самую долю секунды, когда спутник начал ломаться. Инженер больше не смотрит на графики глазами сутками, ИИ сам подсвечивает ему красным опасные зоны на дашборде.
