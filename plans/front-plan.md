# 🌐 План разработки Frontend (apps/orbit-front)

Этот документ описывает детальную структуру, страницы, компоненты и стейт-менеджмент для клиентского приложения OrbitAI на базе Next.js (App Router).

## 1. 📂 Структура Роутинга (Страницы)

Система построена на папке `app/`, где каждая директория — это отдельный маршрут (Route).

*   [ ] 🔴 **`app/page.tsx` (Landing Page)**
    *   Продающий лендинг для B2B-клиентов.
    *   **Блоки:** Hero (Заголовок "Оцифруйте космос"), Блок "Как это работает" (3 шага), Калькулятор окупаемости (приманка), Футер с контактами.
    *   **Действие:** Кнопка "Начать бесплатно" -> Редирект на `/auth/register`.

*   [ ] 🔴 **`app/auth/login/page.tsx` (Вход)**
    *   Форма авторизации (Email/Пароль).
    *   Кнопка "Войти через Google" (Supabase OAuth).

*   [ ] 🔴 **`app/auth/register/page.tsx` (Регистрация)**
    *   Форма создания аккаунта.

*   [ ] 🔴 **`app/dashboard/layout.tsx` (Рабочее пространство - Layout)**
    *   Общая оболочка для всех закрытых страниц.
    *   **Включает:** Боковой Sidebar (навигация по модулям), Верхний Header (профиль юзера, баланс токенов).
    *   *Важно:* Этот Layout содержит проверку авторизации (Middleware) — редирект на `/auth`, если юзер не вошел.

*   [ ] 🔴 **`app/dashboard/page.tsx` (Главная Дашборда - Карта)**
    *   Центральный элемент системы — большая интерактивная 2D/3D карта (Leaflet или Mapbox).
    *   Здесь юзер визуально выделяет территории (BBox) для расчетов.

*   [ ] 🔴 **`app/dashboard/mission-designer/page.tsx`**
    *   Чат-подобный интерфейс или умная форма NLP. Ввод текстом: "Следить за мостом в Крыму раз в сутки". Вывод: Сгенерированное ТЗ для спутника.

*   [ ] 🔴 **`app/dashboard/value-predictor/page.tsx`**
    *   Панель предсказания ценности. Выбор зоны на карте -> Настройка параметров (погода, сезон) -> График потенциального дохода.

*   [ ] 🔴 **`app/dashboard/reports/page.tsx`**
    *   История сгенерированных отчетов в виде таблицы/сетки файлов PDF. Скачивание и предпросмотр.

## 2. 🧩 Общие UI Компоненты (`src/components/`)

Используем **Shadcn UI** (Radix UI + Tailwind) для консистентного и быстрого дизайна.

*   [ ] 🔴 **`ui/` (Базовые элементы Shadcn):**
    *   `Button` (Кнопки)
    *   `Input` / `Textarea` (Текстовые поля)
    *   `Card` (Карточки для графиков и отчетов)
    *   `Dialog` (Модальные окна)
    *   `Select` / `DropdownMenu` (Выпадающие списки)
    *   `Toast` (Системные уведомления: "Успех", "Ошибка")
*   [ ] 🔴 **`map/` (Картография):**
    *   `InteractiveMap` (Контейнер карты)
    *   `DrawControl` (Инструменты рисования квадратов/полигонов на карте)
*   [ ] 🔴 **`charts/` (Аналитика):**
    *   Интеграция с `Recharts` или `Chart.js` для красивых графиков Value Predictor или Launch Delays.

## 3. 🧠 State Management (Zustand)

Для глобального хранения данных между страницами (`src/store/`).

*   [ ] 🔴 **`useAuthStore`:**
    *   Хранит `user` (данные профиля), `session` (токен), `isLoading`.
*   [ ] 🔴 **`useMapStore`:**
    *   Хранит `currentBBox` (выделенные координаты на карте).
    *   `selectedLayer` (Спутник, Инфракрасный, Радарный).
*   [ ] 🔴 **`useAppStore`:**
    *   Глобальные настройки интерфейса (Dark/Light тема, открыт ли Sidebar).

## 4. 🔌 Взаимодействие с API (Axios/Fetch)

Логика общения с нашим Node.js Gateway (`src/lib/api/`).

*   [ ] 🔴 **`api.client.ts`:**
    *   Настроенный `Axios` инстанс. Автоматически подмешивает JWT-токен Supabase в `Authorization: Bearer <token>` во все POST/GET запросы.
*   [ ] 🔴 **`map.service.ts`:** Получение тайлов, отправка координат на сервер для оценки.
*   [ ] 🔴 **`auth.service.ts`:** Логика входа/выхода (обертка над Supabase Client).

## 5. 🎨 Дизайн и Стилистика (Tailwind)

*   [ ] 🔴 Настроить `tailwind.config.ts`:
    *   Палитра цветов: "Космические" (Глубокий синий/черный фон, неоновые акценты — Cyan, Purple).
    *   Светлая (опционально) и Темная (по умолчанию) темы классами `.dark`.
