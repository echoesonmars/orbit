# 🎲 Модуль 9: Scenario Simulator (Симулятор Сценариев "Что если?")

**Scenario Simulator** — это инвестиционная песочница платформы OrbitAI. 
Спутниковый бизнес требует гигантских вложений (от $5 млн до $500 млн) и несет огромные риски: ракета может взорваться, камера может сломаться, облачность над нужным городом может висеть 3 месяца.

Цель этого модуля — проиграть будущее 10,000 раз и сказать инвестору: *"Шанс, что этот проект станет прибыльным через 3 года, составляет 68%."*

Здесь мы используем не Машинное Обучение, а **Стохастическое Моделирование (Метод Монте-Карло)**.

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Симуляция на 10,000 виртуальных жизней спутника генерирует миллионы точек данных. Нам не нужно хранить каждую виртуальную день сурка, мы храним только итоговые агрегированные результаты (процентили).

**Задачи по настройке БД (Supabase):**
*   [ ] Создать таблицу `simulation_results`:
    *   `id` (Уникальный ID).
    *   `mission_id` (Связь с Техническим заданием из Модуля 2).
    *   `parameters_json` (Входные данные: Бюджет = 10M, Стоимость запуска = 2M, Вероятность поломки = 10%).
    *   `p50_roi` (Медианный ROI - Возврат инвестиций. То, что случается в 50% случаев).
    *   `p90_roi` (Худший сценарий - То, что гарантированно произойдет в 90% случаев).
*   [ ] Подготовить очереди в **Redis**. Метод Монте-Карло — чистая математика, которая грузит процессор на 100%. Это строгий кандидат для **Background Worker'а**, чтобы не вешать Gateway.

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI / Worker)

Этот модуль (`services/worker/tasks/simulator.py`) — это финансово-вероятностный калькулятор.

**Задачи по разработке математики (Monte Carlo Simulation):**
*   [ ] **Интеграция с NumPy/SciPy:** Это лучшие библиотеки для генерации случайных чисел с нормальным или логнормальным распределением.
*   [ ] **Цикл Монте-Карло:** Написать функцию-цикл (`for loop`), которая крутится 10,000 раз:
    1.  *Событие 1 (Запуск):* Генератор случайных чисел кидает кубики. Выпало 95. Ура, ракета взлетела! Минус $2M из бюджета. (Если бы выпало <5, ракета бы взорвалась $\rightarrow$ проект закрыт).
    2.  *Событие 2 (Жизнь спутника, 365 дней):* Каждый "день" кидаем кубики на Погоду, Поломку и Качество снимков. 
    3.  *Событие 3 (Доход):* Если погода ясная, мы виртуально "снимаем" Землю (используя оценку из Модуля 3) и кладем виртуальные $1,250 в копилку.
    4.  *Событие 4 (Итог жизни №1):* За год мы заработали $12M. Пишем результат в таблицу и начинаем жизнь №2 заново.
*   [ ] **Расчет Квантилей (Pandas):** Из 10,000 результатов мы берем массив итоговых заработков, отсортировываем его и достаем значения P10 (Лучший сценарий на Уолл-Стрит), P50 (Реальность) и P90 (Кошмарный сценарий).

---

## 3. 🤖 Почему здесь метод Монте-Карло, а не ML?

### 🎓 БАЗА: Монте-Карло (Управление рисками)
Представьте, что вы кидаете кубик 1 раз. Если выпадет 6 — вы миллионер, если 1 — банкрот. Это огромный риск.
Как предсказать ваше будущее? ML здесь не поможет (у кубика нет памяти, нет исторического контекста). 
Но если мы попросим компьютер кинуть виртуальный кубик 10,000,000 раз, мы увидим Идеальное Распределение Рисков (Колокол Гаусса). И мы сможем точно сказать: *"Вероятность того, что вы будете в плюсе — 33.3%"*. 

**Как работают переменные для симуляции:**
Инвестор вводит начальные "кубики":
*   Стоимость запуска: от \$1M до \$3M (В среднем \$1.5M).
*   Вероятность потери связи со спутником: 2% в год.
*   Количество ясных дней в целевом регионе: от 30 до 120 (Берем из Data Hub).

Python крутит свою рулетку Монте-Карло и выдает Инвестору "Коридор Рисков" (Fan Chart / Конус Неопределенности). Это то, на что смотрят страховые компании (Сколько будет стоить страховка этого спутника?).

---

## 4. 🌐 Интеграция с Frontend и Gateway

Для пользователя это должна быть интерактивная песочница с ползунками, дающая быстрый ответ.

*   [ ] **Frontend (Next.js):**
    *   Создать страницу `app/dashboard/simulator/page.tsx`.
    *   Слева панель: множество ползунков (Бюджет, Риск ракеты, Стоимость гигабайта данных связи).
    *   Пользователь нажимает "Запустить Симуляцию". 
    *   Так как это фоновая задача (через Worker), показываем красивый ползунок загрузки ("Симуляция 1200 / 10000 жизней...").
    *   Справа рисуем крутой **График распределения (Histogram)** или **Конус Неопределенности (Fan Chart в Recharts/ApexCharts)**. Центр конуса (зеленый) — медианная прибыль. Края (красные) — риск банкротства.
*   [ ] **Node.js (Gateway):**
    *   Эндпоинт `POST /api/simulate`. Шлет задачу в Redis, возвращает `job_id`.
    *   Эндпоинт `GET /api/simulate/:job_id/progress` для того чтобы ползунок на сайте полз плавно от 0 до 100%.

---

**Резюме:** Девятая функция — это инструмент инвестиционных банкиров и страховых брокеров космической отрасли. Она строится не на Data Science в классическом понимании (где мы ищем паттерны), а на **Актуарной Математике и Теории Вероятностей**. С помощью симулятора "Случайных блужданий" (Монте-Карло), Python берет все наши параметры (от ракет и погоды) и рисует инвестору честный математический коридор того, сколько он заработает через три года: от банкротства до IPO.
