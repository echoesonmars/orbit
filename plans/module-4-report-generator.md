# 📄 Модуль 4: Report Generator (Генератор Аналитических Отчетов)

**Report Generator** — это "лицо" нашей платформы OrbitAI для конечного заказчика. 

Даже если наши ML-модели из Модуля 3 работают идеально и выдают цифру "$1,250", бизнесу и инвесторам этого недостаточно. Им нужен **Explainable AI (Объяснимый ИИ)** — красивый, брендированный PDF-документ на 5-10 страниц, где с графиками, картами и текстом разжевано, *почему* ИИ принял такое решение, какие есть риски (погода, задержки) и каковы шансы на успех.

Этот модуль переводит сухие JSON-ответы из других сервисов в профессиональный бизнес-отчет.

---

## 1. 🗄 Что нужно настроить (База Данных / Инфраструктура)

Мы не будем хранить сами PDF-файлы внутри таблиц PostgreSQL. Бинарные файлы (картинки, документы) должны лежать в объектном хранилище (Storage), а в базе мы сохраняем только ссылку на файл.

**Задачи по настройке (Supabase):**
*   [ ] **Supabase Storage (S3-совместимое хранилище):** Создать бакет (корзину) `mission_reports`. Настроить приватность (чтобы чужие отчеты нельзя было скачать без авторизации).
*   [ ] **Таблица `generated_reports` (PostgreSQL):**
    *   `id` (Уникальный UUID отчета).
    *   `user_id` (Кто заказал).
    *   `mission_id` (Связь с проектом из Модуля 2).
    *   `file_url` (Ссылка на PDF в бакете `mission_reports`).
    *   `status` (Статус генерации: `processing`, `completed`, `failed`).

---

## 2. 🐍 Что нужно написать (Код на Python / FastAPI / Worker)

Генерация красивого многостраничного PDF с графиками и картами — это ресурсоемкий процесс. Если мы засунем это в основной API-эндпоинт FastAPI, сервер "подвиснет" на 5-10 секунд, и другие пользователи не смогут работать. 
Поэтому этот модуль идеально подходит для **Background Worker'а (Фоновых задач)** через брокер очередей Redis.

*   `services/worker/tasks/report_tasks.py` (Отдельный Python-процесс, слушающий Redis)

**Задачи по разработке:**
*   [ ] **Шаблонизатор HTML/PDF:** Использовать связку `Jinja2` (для создания HTML-шаблона отчета с красивым CSS) + `WeasyPrint` или `pdfkit` (для конвертации HTML в PDF).
*   [ ] **Генерация визуализаций:** Внутри Python-воркера использовать `Matplotlib` или `Plotly` для отрисовки графиков прогноза (из Модуля 3) в виде PNG-картинок, которые затем вставляются в HTML.
*   [ ] **Рендер карты (Static Map):** Интеграция с API Mapbox (Static Images API). Воркер берет координаты (BBox) и запрашивает у Mapbox картинку карты с нарисованным квадратом, чтобы вставить её в шапку отчета.
*   [ ] **LLM-Саммаризация (OpenAI):** Прогнать сухие метрики через GPT-4, чтобы сгенерировать абзац "Executive Summary" (Краткий вывод человеческим языком) на первой странице отчета.
*   [ ] **Загрузка в Облако:** Функция загрузки готового PDF в Supabase Storage и обновления статуса в БД через `supabase-py`.

---

## 3. 🤖 Что нужно обучать (ML & Data Science)

В этом модуле нет классического машинного обучения (обучения на таблицах), но здесь применяется **Generative AI (Генеративный ИИ)** и **Explainable AI (XAI)**.

### 🎓 БАЗА: Как ИИ объясняет сам себя?
Самая большая проблема ML (например, в Модуле 3) — это эффект "Черного ящика". Модель выдает цену $1,250, но инвестор спрашивает: *"Почему так мало?"*

Для этого Data Scientist использует техники **XAI (SHAP или LIME)**:
*   [ ] **SHAP (SHapley Additive exPlanations):** Это математическая библиотека в Python. Мы скармливаем ей прогон нашей ML-модели, и она раскладывает итоговое предсказание на кусочки.
*   SHAP возвращает массив: `Базовая цена: $1,000`. `Фактор "Дождь": -$200`. `Фактор "Срочность": +$450`. 
*   Именно эти цифры из библиотеки SHAP воркер берет и рисует в виде красивой диаграммы (Waterfall Chart) на 3-й странице PDF-отчета. Инвестор визуально видит, какой фактор сыграл в плюс, а какой в минус.

---

## 4. 🌐 Интеграция с Frontend и Gateway

Так как генерация PDF занимает время (отрисовка графиков, скачивание карт, ответ от OpenAI), фронтенд не должен зависать в ожидании ответа. Мы реализуем асинхронный паттерн (Polling или WebSockets).

*   [ ] **Node.js (Gateway):**
    *   Эндпоинт `POST /api/reports/generate`. Шлюз просто кидает задачу в очередь Redis, мгновенно отвечает фронтенду `{"status": "processing", "task_id": 123}` и забывает о ней. Этим займется Python Worker.
    *   Эндпоинт `GET /api/reports/:id/status`. Для проверки готовности.
*   [ ] **Frontend (Next.js):**
    *   Пользователь нажимает на Дашборде кнопку **"Сгенерировать PDF-отчет"**.
    *   Кнопка переходит в состояние спиннера (загрузки).
    *   Каждые 2 секунды (через React Query или `setInterval`) фронтенд спрашивает `Gateway`: *"Отчет готов?"*.
    *   Как только статус меняется на `completed`, фронтенд показывает красивую зеленую галочку и скачивает файл из Supabase Storage по прямой ссылке, открывая PDF в новой вкладке.
    *   Также нужно создать страницу `app/dashboard/reports/page.tsx`, где в виде таблицы (Shadcn Table) будет лежать вся история когда-либо сгенерированных отчетов пользователя.

---

**Резюме:** Четвертая функция — это мостик между тяжелой математикой серверов и деловым миром. Она собирает данные из всех предыдущих ИИ-модулей (орбиту из Модуля 2, прогноз цены из Модуля 3, объяснение "черного ящика" через SHAP) и компилирует их в многостраничный PDF-отчет. 
Самое важное архитектурное решение здесь — вынести этот "тяжелый" процесс за пределы основного API (FastAPI) в фоновый сервис (Worker + Redis), чтобы обеспечить мгновенный отклик интерфейса (Next.js) для пользователей.
