# 🎨 OrbitAI: UI/UX Style Guide & Design Principles

Этот документ описывает визуальный язык, правила верстки и компоненты برای создания премиального, "вау" интерфейса платформы **OrbitAI**. 

Наша цель: создать ощущение профессионального, дорогого и футуристического инструмента аналитики уровня SpaceX или Palantir.

---

## 1. 🌌 Базовая Концепция (Vibe)
Интерфейс должен вызывать ассоциации с космосом, пустотой, точной физикой и искусственным интеллектом.
*   **Deep Dark Mode:** Интерфейс по умолчанию темный. Светлой темы пока нет.
*   **Glassmorphism (Матовое стекло):** Панели и карточки поверх карты Земли должны быть полупрозрачными с легким эффектом размытия (blur), чтобы карта просвечивала.
*   **Неоновые акценты:** Тонкие линии, подсветки (glow) и акцентные кнопки должны светиться на черном фоне.
*   **Whitespace (Воздух):** Много пустого пространства вокруг текста и графиков. Интерфейс дышит, он не перегружен рамками.

---

## 2. 🎨 Цветовая Палитра (Tailwind)

Забудьте про стандартный серый `bg-gray-900`. Мы используем глубокие сине-черные тона.

*   `background` (Главный фон): `#0A0E17` (Глубокий синий/черный космос).
*   `surface` (Карточки/Модалки): `#111827` с прозрачностью (например `bg-slate-900/60`).
*   `border` (Границы окон): Очень тонкий, полупрозрачный белый/синий `#ffffff1a`. 
*   **Акценты (Primary/Neon):**
    *   **Electric Blue (Главный):** `#00F0FF` (Используется для кнопок, активных табов, фокуса).
    *   **Space Purple (ML/AI):** `#9D4EDD` (Используется везде, где работает ИИ или Модуль 2).
*   **Семантические цвета (Status):**
    *   Успех (Grade A): `#10B981` (Изумрудный, не кислотный).
    *   Ошибка (Grade F / Задержки): `#EF4444` (Сигнальный красный).

---

## 3. 📝 Типографика (Шрифты)

Мы отходим от стандартных браузерных шрифтов для придания технологичности.

*   **Заголовки (Headlines - <h1>, <h2>):** 
    *   Шрифт: `Outfit` (или `Space Grotesk`).
    *   Стиль: Геометричный, слегка сплюснутый, идеально подходит для интерфейсов будущего.
    *   Вес: `Medium` (500) или `SemiBold` (600).
*   **Наборный текст и Таблицы (Body):**
    *   Шрифт: `Geist` (используется по умолчанию для обычного текста).
    *   Украшения и декоративные элементы: `Geist Mono` (технические значения, код, метки).
    *   Стиль: Обычный текст `Geist` всегда должен иметь `letter-spacing: -0.05em` (уже настроено в `globals.css`) для большей плотности.
    *   *Важно:* Для всех цифр (цена, высота орбиты, баллы) использовать моноширинный вариант `tabular-nums` в Tailwind, чтобы графики не "прыгали", когда цифры меняются.

---

## 4. 🧩 Работа с Компонентами (Shadcn + Magic UI)

Мы используем базу Shadcn UI, но "докручиваем" её стили под наш Vibe. И так же интегрируем красивые элементы из Magic UI.

**Правила кастомизации:**
1.  **Радиусы скругления (Border Radius):** Мы не используем сильно круглые формы.
    *   Формы, Кнопки: `rounded-md` (Среднее скругление, строгий бизнес).
    *   Карточки, Дашборды: `rounded-xl` или `rounded-2xl` (Мягкие углы для фоновых блоков).
2.  **Кнопки (Buttons):**
    *   *Primary (Главное действие):* Фон `bg-transparent`, рамка `border border-cyan-400`, текст цвета циана `text-cyan-400`.
    *   *Hover эффект (Самое важное!):* При наведении на Primary кнопку должен появляться Glow-эффект. В Tailwind это: `hover:bg-cyan-400/10 hover:shadow-[0_0_15px_rgba(0,240,255,0.4)]`.
3.  **Карточки (Cards):**
    *   Никаких глухих заливок. Используйте Glassmorphism:
    *   Классы Tailwind: `bg-slate-900/50 backdrop-blur-xl border border-white/10`.
4.  **Shadows (Тени):**
    *   Запрещено использовать черные тени `shadow-xl` на темном фоне (их не видно, и они грязные).
    *   Используйте "световые" тени-свечения для акцентов (цветные `box-shadow`).

---

## 5. 🎬 Анимации и Микро-взаимодействия (Framer Motion)

Статичный интерфейс кажется дешевым. Мы оживляем его, но *очень* деликатно.

*   **Появление страниц (Page Load):**
    *   Контент внутри Дашборда не должен просто моргать при загрузке.
    *   *Паттерн:* Плавное всплытие снизу вверх с небольшим затуханием (Fade-up).
    *   Настройки Framer Motion: `initial={{ opacity: 0, y: 20 }}` -> `animate={{ opacity: 1, y: 0 }}` (transition duration: 0.4s).
*   **Loading States (Ожидание ИИ):**
    *   Модули 3, 4 и 9 (отчеты и симуляции) думают долго (до 5-10 сек).
    *   *Запрещено:* Крошечные скучные "крутилки" (spinners).
    *   *Разрешено:* Красивые бегущие градиентные полосы (Magic UI Meteors), пульсирующие скелетоны (Shadcn Skeleton) или прогресс-бары с текстом *"Инициализация генератора Монте-Карло... 45%"*.
*   **Hover-эффекты:**
    *   Строки в таблицах запусков ракет должны слегка подсвечиваться при наведении мышки (`hover:bg-white/5`), чтобы таблица казалась живой.

---

## 6. 🗺 Layout и Эргономика (Где что находится)

1.  **Гегемония Карты (Dashboard Layout):**
    *   Карта Земли (Leaflet/Mapbox) с темно-синим базовым слоем всегда находится на слое `z-0` и занимает 100% экрана.
    *   Все меню, кнопки и графики — это полупрозрачные плавающие (Floating) панели с `z-10` сверху карты.
2.  **Sidebar (Навигация):**
    *   Тонкая левая панель с иконками 10 модулей платформы. Сворачиваемая, чтобы дать карте больше места.
3.  **Right Panel (Конфигуратор):**
    *   Правая панель (или Drawer) выезжает только когда пользователь кликнул куда-то (например, нажал "Запустить симуляцию" или "Оценить снимок"). Там лежат ползунки и формы ввода.

## 7. 🧱 Компонентный подход (Строгое Правило)

**Вся верстка платформы должна быть на 100% компонентной.**

1.  **Запрещено писать прямо в страницах:** Не создавайте огромные блоки `<div className="...">...</div>` внутри файлов маршрутов (`app/page.tsx`, `app/dashboard/page.tsx`). Страница должна быть чистой и играть только роль связующего звена.
2.  **Создавайте и переиспользуйте:** Если вам нужно сверстать любую секцию, карточку или сложный блок — вы **обязаны** создать кастомный компонент в `components/` или использовать готовые из Magic UI и Shadcn UI.
3.  **Их интеграция:** Собирайте страницу из готовых блоков, например: `<HeroSection />`, `<PricingTable />`, `<FeatureCard />`.

---
**Итог:** OrbitAI — это не скучная бухгалтерская CRM. Это командный мостик из фантастических фильмов. Идеальный баланс между строгими, читаемыми математическими графиками и завораживающим глубоким "космическим" дизайном (Glass, Glow, Smooth Motion). И все это собрано из элегантных, изолированных компонентов.
