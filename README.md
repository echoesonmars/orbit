# 🚀 OrbitAI

**OrbitAI** — это модульная AI-платформа для принятия решений и монетизации космических данных. Платформа переводит сложные инженерные расчеты космических миссий в понятные бизнес-метрики, оценивает коммерческую ценность спутниковых снимков до их создания и генерирует технические задания с помощью ИИ.

## 🛠 Технологический стек (Монорепозиторий)

Проект построен по принципу **Модульного Монорепозитория** с микросервисной архитектурой.

### 1. Frontend (`apps/orbit-front`)
- **Фреймворк:** Next.js (App Router)
- **Язык:** TypeScript, React
- **Стилизация:** Tailwind CSS v3
- **Карты / UI:** Leaflet/Mapbox, Shadcn UI, Zustand

### 2. API Gateway (`services/gateway`)
- **Среда:** Node.js, Express
- **Язык:** TypeScript
- **Валидация данных:** Zod
- **Роль:** Принимает внешний трафик, проверяет аутентификацию (Supabase JWT) и перенаправляет вычислительные запросы в Python-микросервисы.

### 3. ML Core & Расчеты (`services/ml-api`)
- **Фреймворк:** Python 3.11, FastAPI
- **Валидация:** Pydantic
- **ML/Математика:** Scikit-learn, LightGBM, Rasterio, Geopandas
- **Интеграции:** OpenAI API
- **Роль:** Внутренний сервис для обработки геоданных, математического моделирования и запуска ML-моделей. Недоступен напрямую извне сети.

### 4. Background Worker (`services/worker`)
- Отдельный сервис (Python) для длительных ресурсоемких ML-задач, работающий через очереди (Redis).

### 5. Shared Contracts (`shared/`)
- Общие TypeScript интерфейсы и схемы ответов для синхронизации моделей данных между Frontend и Backend-сервисами.

### 6. Infrastructure (`infra/`)
- Docker, Docker Compose
- **База Данных:** Supabase (PostgreSQL + PostGIS), настроенная через миграции (`infra/db`).
- Redis для очередей задач.

## 📂 Дополнительная документация

* **Архитектурные требования и список ИИ-модулей:** см. файл `details.md`
* **Детальное дерево файловой структуры:** см. файл `file_structure.md`

## 🚀 Быстрый старт (Development)

Для локального запуска убедитесь, что у вас установлены `Node.js`, `Python 3.11+` и конфигурация для запуска Docker контейнеров.

```bash
# 1. Запуск инфраструктуры (базы данных, кэш)
docker-compose up -d

# 2. Переход во фронтенд
cd apps/orbit-front
npm install
npm run dev

# 3. Настройка и запуск сервисов (Gateway и ML-API)
# (Дополнительные инструкции для настройки микросервисов)
```
